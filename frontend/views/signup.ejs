<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup</title>
    <style>
      body {
          font-family: Arial, sans-serif;
          background-color: #f9f9f9;
          margin: 0;
          padding: 0;
      }
      .banner {
          background-color: #2ecc71; /* Forest green */
          color: #fff;
          padding: 10px;
          text-align: center;
          border-radius: 8px 8px 0 0;
          transition: background-color 0.3s ease; /* Added background color transition */
          position: relative; /* Added position relative for absolute positioning */
      }
      .logo {
          position: absolute; /* Position the logo absolutely */
          top: 50%; /* Adjust top position */
          transform: translateY(-50%); /* Center vertically */
          left: 10px; /* Adjust left position */
          width: 40px; /* Adjust width as needed */
          height: auto;
      }
      /* Dark Mode Button */
      .dark-mode-btn {
          position: absolute;
          top: 10px; /* Adjust top position */
          right: 10px; /* Adjust right position */
          background-color: transparent;
          color: #7f7f7f;
          border: 1px solid #7f7f7f;
          padding: 5px 10px;
          border-radius: 4px;
          cursor: pointer;
          transition: background-color 0.3s ease; /* Added color transition */
      }

      .dark-mode-btn:hover {
          background-color: #333;
          color: #fff; /* White text on hover */
      }
      /* Dark Mode */
      body.dark-mode {
          background-color: #333; /* Dark gray */
      }
      .banner.dark-mode {
          background-color: #004d00; /* Dark green */
          border-bottom: 2px solid #666; /* Dark gray */
          color: #fff; /* White text in dark mode */
      }
      input[type="text"], input[type="password"], select {
          width: 100%;
          padding: 10px;
          margin: 5px 0;
          border: 1px solid #dddddd;
          border-radius: 4px;
          box-sizing: border-box;
      }
      input[type="submit"] {
          width: 100%;
          background-color: #2ecc71; /* Matched button color with banner */
          color: white; /* White text */
          padding: 14px 20px;
          margin: 8px 0;
          border: none;
          border-radius: 4px;
          cursor: pointer;
          transition: background-color 0.3s ease; /* Added color transition */
      }

      .container {
          max-width: 400px;
          padding: 20px;
          background-color: #ffffff;
          border-radius: 8px;
          box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
          margin: 50px auto;
      }

      .title-box {
          background-color: #2ecc71; /* Matched banner color */
          color: #fff;
          text-align: center;
          padding: 10px;
          border-radius: 8px 8px 0 0;
          margin-bottom: 20px;
      }

      .title {
          font-family: 'Arial Black', sans-serif;
          font-size: 24px;
          margin: 0;
      }

      form {
          text-align: center;
      }
      /* Styling for radio buttons */
      input[type="radio"] {
          /* Hide default radio button */
          appearance: none;
          -webkit-appearance: none;
          -moz-appearance: none;
          width: 20px; /* Size of custom radio button */
          height: 20px; /* Size of custom radio button */
          border: 2px solid #2ecc71; /* Border color */
          border-radius: 50%; /* Make it round */
          outline: none; /* Remove outline */
          cursor: pointer; /* Show pointer cursor */
          margin-right: 5px; /* Add some spacing between radio button and label */
      }
      
      /* Styling for checked state of radio buttons */
      input[type="radio"]:checked {
          background-color: #2ecc71; /* Fill color when checked */
      }

      input[type="text"],
      input[type="password"],
      button {
          width: 100%;
          padding: 10px;
          margin-bottom: 10px;
          border: 1px solid #dddddd;
          border-radius: 5px;
          box-sizing: border-box;
          font-size: 16px;
      }

      button {
          background-color: #2ecc71; /* Matched banner color */
          color: #fff; /* White text */
          cursor: pointer;
      }

      button:hover {
          background-color: #27ae60; /* Darker green on hover */
      }

      .other-action {
          text-align: center;
          margin-top: 10px;
      }

      .other-action a {
          text-decoration: none;
          color: #555555;
          background-color: #dddddd; /* Gray background */
          padding: 10px 20px; /*  Adjust padding for button-like appearance */
          border-radius: 5px; 
      }

  </style>
</head>
<body>
  <div class="banner">
    <img class="logo" src="frontend/views/logo.png" alt="Logo">
    <h1>Classroom Panel</h1>
    <button class="dark-mode-btn" onclick="toggleDarkMode()">Toggle Dark Mode</button>
  </div>
    <div class="container">
        <h2>Signup</h2>
        <form method="post" action="/signup "id="signupForm">
            <input type="text" name="username" placeholder="Username" required>
            <input type="password" name="password" placeholder="Password" required>
            <input type="radio" name="role" value="student" default="checked"> Student
            <input type="radio" name="role" value="teacher"> Teacher
            <button type="submit">Signup</button>
        </form>
        <div class="other-action">
            <a href="/login">Login Instead</a>
        </div>
    </div>
    <script>
      function toggleDarkMode() {
          document.body.classList.toggle('dark-mode');
          var banners = document.querySelectorAll('.banner');
          banners.forEach(banner => {
              banner.classList.toggle('dark-mode');
          });
      }
  </script>
</body>
</html>
