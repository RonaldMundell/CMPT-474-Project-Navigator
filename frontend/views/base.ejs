<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project Navigator</title>
    <style>
        /* Updated CSS for styling */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9;
            transition: background-color 0.3s ease; /* Added background color transition */
            color: #333; /* Default text color */
        }
        
        .container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin: 50px auto;
            align-items: left;
            text-align: left;
            /* margin-left: auto; */
            /* margin-right: auto; */
        }

        input[type="text"], input[type="password"], select {
            width: 100%;
            padding: 10px;
            margin: 5px 0;
            border: 1px solid #dddddd;
            border-radius: 4px;
            box-sizing: border-box;
        }
        input[type="submit"] {
            width: 100%;
            background-color: #2ecc71; /* Matched button color with banner */
            color: white; /* White text */
            padding: 14px 20px;
            margin: 8px 0;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease; /* Added color transition */
        }
        input[type="submit"]:hover {
            background-color: #27ae60; /* Darker green on hover */
        }
        label {
            color: #333333;
            margin-bottom: 5px;
            display: block;
        }
    </style>
</head>
<body>
    <!-- Include Navbar -->
    <%- include('partials/navbar') %>

    <!-- Content -->
    <div class="container">
        <h2>Welcome back <%= displayName %> </h2>
    </div>

    <div class="container">
        <h2>Create Classroom</h2>
        <form method="post" id="create-classroom-form">
            <label for="classroom">Classroom Name:</label>
            <input type="text" id="classroom" name="classroom" placeholder="Enter classroom name" required>
            <label for="teacher">Teacher's Name:</label>
            <input type="text" id="teacher" name="teacher" placeholder="Enter teacher's name" required>
            <input type="submit" value="Create Classroom">
        </form>
    </div>

    <div class="container">
        <h2>Add Students</h2>
        <form method="post" id="add-student-form">
            <label for="studentName">Student's Name:</label>
            <input type="text" id="student-name" name="studentName" placeholder="Enter student's name" required>
            <label for="classroomCode">Classroom Code:</label>
            <input type="text" id="classroom-code" name="classroomCode" placeholder="Enter classroom code" required>
            <input type="submit" value="Add Student">
        </form>
    </div>
</body>
</html>
